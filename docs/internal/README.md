# Internal development notes

These notes cover loader implementation and reverse-engineering work rather than the public mod API. They are intentionally outside the user-facing mdBook navigation. The maintained public documentation lives in `docs/src-en` and `docs/src-de`.
