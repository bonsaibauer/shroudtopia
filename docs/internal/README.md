# Internal development notes

These notes cover loader implementation and reverse-engineering work rather than the public mod API. They are intentionally outside the user-facing mdBook navigation. The maintained public documentation lives in paired `*_en.md` and `*_de.md` files in `docs/src`.
