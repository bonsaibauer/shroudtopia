# Events

Abonniere Events mit `EventSubscription` und veröffentliche sie mit `Event`. Payload-Speicher gilt nur für die dokumentierte Callback-Lebensdauer und muss kopiert werden, wenn er länger benötigt wird.

Siehe [Events-Referenz](../reference/events.md).
