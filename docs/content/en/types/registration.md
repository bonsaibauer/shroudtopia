# Registration

`Registration` identifies an ownership-bound registered resource. Store successful registrations and release them with `release_registration`. The loader performs owner-wide cleanup internally after unload.
