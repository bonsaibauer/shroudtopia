# Events

Subscribe with an `EventSubscription` and publish with an `Event`. Payload memory remains subject to the documented callback lifetime; copy it when it must outlive the callback.

See the [Events reference](../reference/events.md).
