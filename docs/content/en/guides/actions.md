# Actions

Actions expose discoverable operations. Describe accepted JSON with `input_schema_json`, validate inputs in the handler, and report current state through the host API.

See the [Actions reference](../reference/actions.md).
